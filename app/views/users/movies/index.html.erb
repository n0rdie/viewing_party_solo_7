<%= button_to 'Discover', "/users/#{@user.id}/discover", method: :get %>
<% @movies.each do |movie| %>
    <h4><%= link_to "#{movie[:title]}", "/users/#{@user.id}/movies/#{movie[:id]}" %> || <%= movie[:vote_average] %></h4>
<% end %>